<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="match_parent">

    <EditText
        android:layout_width="match_parent"
        android:layout_height="250dp"
        android:id="@+id/chairman_msg_edit"
        android:background="@drawable/texteditor"
        android:layout_marginTop="42dp"
        android:layout_marginLeft="5dp"
        android:layout_marginRight="5dp"
        android:hint="Give Message to Reception"
        android:textColor="@color/colorPrimary"
        android:textColorHint="@color/colorPrimary"
        android:textAlignment="center"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:text="To Reception"
        android:background="@drawable/login_btn"
        android:id="@+id/chairman_msg_btn"
        android:layout_marginTop="62dp"
        android:layout_below="@+id/chairman_msg_edit"
        android:layout_centerHorizontal="true"
        android:textColor="#ffffff" />
</RelativeLayout>